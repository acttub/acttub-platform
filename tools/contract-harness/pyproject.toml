[project]
name = "contract-harness"
version = "0.1.0"
description = "FastAPI → Spring Boot 이관용 계약 동등성 하네스 (M6에서 폐기)"
requires-python = ">=3.11"
# 의존성을 새로 들이지 않는다. apps/api 의 uv 워크스페이스 가상환경을 그대로 쓴다.
dependencies = []

[dependency-groups]
dev = []

[tool.pytest.ini_options]
testpaths = ["tests"]
