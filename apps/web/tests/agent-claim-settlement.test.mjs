import assert from "node:assert/strict";import test from "node:test";import {settleAgentClaimProgress} from "../src/server/agent-claim-settlement.js";
const run=(status="running",safeErrorCode=null)=>({id:"r",status,safeErrorCode});const base={owned:true,run:run()};const args={claimed:base,expectedSubstantiveAnswerCount:0,expectedTotalConversationCount:0,actorTurnCount:s=>s.total,staleError:()=>Object.assign(new Error("stale"),{code:"STALE_INTERVIEW_PROGRESS"}),persistenceError:()=>Object.assign(new Error("persist"),{code:"TURN_PERSISTENCE_FAILED"})};
test("initial aggregate failure fails closed before dispatch",async()=>{await assert.rejects(settleAgentClaimProgress({...args,readAggregate:async()=>{throw new Error("db")},failRun:async()=>{}}),e=>e.code==="TURN_PERSISTENCE_FAILED")});
test("race to exact failed returns persisted run and safe code",async()=>{const result=await settleAgentClaimProgress({...args,readAggregate:async()=>({runs:[run("failed","AI_TIMEOUT")],substantiveAnswerCount:1,total:1}),failRun:async()=>{}});assert.equal(result.run.safeErrorCode,"AI_TIMEOUT");assert.equal(result.owned,false)});
test("mismatch settles pending or running claim before dispatch",async()=>{let failed=false,calls=0;await assert.rejects(settleAgentClaimProgress({...args,claimed:{owned:false,run:run("pending")},readAggregate:async()=>({runs:[run(failed?"failed":"pending",failed?"AI_INTERNAL":null)],substantiveAnswerCount:1,total:1}),failRun:async()=>{calls+=1;failed=true}}),e=>e.code==="STALE_INTERVIEW_PROGRESS");assert.equal(calls,1)});
