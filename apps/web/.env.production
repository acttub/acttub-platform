# 운영 정적 빌드(next build) 전용 — next dev에는 적용되지 않는다.
# 로컬/개발은 코드 기본값(dev 폼)을 쓰고, 운영 빌드만 Google 로그인을 켠다.
NEXT_PUBLIC_AUTH_PROVIDER=google
# Google Cloud Console에서 발급한 OAuth 클라이언트 ID(공개 값). 발급 후 교체할 것.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=REPLACE_WITH_GOOGLE_CLIENT_ID
