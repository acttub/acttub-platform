# dependencies
node_modules/

# build outputs
.next/
.lighthouseci/
out/
dist/
build/

# env
.env
.env.*

# python 캐시 — 백엔드는 자바지만 apps/mobile/scripts/ 의 폰트·스토어 자산 도구가
# 파이썬이라 남긴다. pytest 는 저장소 어디에도 없어 뺐다(SOMA-403 5단계).
__pycache__/
*.pyc

# logs
*.log
pnpm-debug.log*

# OS/editor
.DS_Store
.idea/
.vscode/

# 에이전트 로컬 상태 (worktree는 체크아웃 사본, 나머지는 개인 설정이라 무시한다)
.claude/worktrees/
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Playwright MCP가 만드는 세션 산출물(스크린샷·트레이스)
.playwright-mcp/

# 에이전트 티켓·작업 메모 (정본은 Jira SOMA — 여기는 로컬 전용)
.scratch/
