packages:
  - "apps/*"
  - "!apps/mobile" # Expo RN 앱 — npm/EAS로 자립 관리(pnpm 심링크가 Metro를 깨서 워크스페이스 제외)
  - "packages/*"

overrides:
  tmp: 0.2.7
  uuid: 11.1.1

allowBuilds:
  sharp: true
  unrs-resolver: true
